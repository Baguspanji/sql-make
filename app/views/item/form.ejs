<%- contentFor('body') %>

    <div class="row">
        <!-- left column -->
        <div class="col-md-12">
            <!-- jquery validation -->
            <div class="card card-lightblue">
                <div class="card-header">
                    <h3 class="card-title">
                        <%= title %>
                    </h3>
                </div>
                <!-- /.card-header -->
                <!-- form start -->
                <form class="needs-validation" action="<%= title.split(' ')[0] == 'Tambah' ? '/item' : '/item/' + data.id %>" method="POST" novalidate>
                    <div class="card-body">
                        <div class="form-group">
                            <label>Nama Barang</label>
                            <input type="text" class="form-control" name="name" required="" value="<%= (typeof data !=='undefined' ) ? data.name : '' %>">
                            <div class="invalid-feedback">
                                Masukkan Nama Barang
                            </div>
                        </div>
                        <div class="form-group">
                            <label>Harga</label>
                            <input type="number" class="form-control" name="price" required="" value="<%= (typeof data !=='undefined' ) ? data.price : '' %>">
                            <div class="invalid-feedback">
                                Masukkan Harga
                            </div>
                        </div>
                        <div class="form-group">
                            <label>Harga Coret</label>
                            <input type="number" class="form-control" name="strike_price" required="" value="<%= (typeof data !=='undefined' ) ? data.strike_price : '' %>">
                            <div class="invalid-feedback">
                                Masukkan Harga Coret
                            </div>
                        </div>
                    </div>
                    <!-- /.card-body -->
                    <div class="card-footer">
                        <button type="submit" class="btn btn-success">
                            <%= title.split(' ')[0] == 'Tambah' ? 'Simpan' : 'Update' %>
                        </button>
                    </div>
                </form>
            </div>
            <!-- /.card -->
        </div>
        <!--/.col (left) -->
        <!-- right column -->
        <div class="col-md-6">

        </div>
        <!--/.col (right) -->
    </div>